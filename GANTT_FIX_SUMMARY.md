# 🏁 간트 차트 의존성 화살표 작업 및 롤백 보고

장욱 님, 요청하신 대로 모든 코드를 커밋 `c0dbc50f2e` 시점으로 롤백하고 구동을 시도했습니다.

## 🛠 작업 내역
1. **강제 롤백**: `git reset --hard c0dbc50f2e` 및 `git clean -fd` 수행
2. **의존성 설치**: `pnpm install` 완료
3. **서버 실행**: `apps/web` 프로젝트를 3000번 포트에서 실행 중 (`npm run dev`)

## ⚠️ 확인된 문제점 (나갔다 오셔서 확인하실 내용)
현재 로컬 개발 환경의 제약으로 인해 화면이 정상적으로 출력되지 않고 있습니다.

### 1. 프런트엔드 빌드 에러 (Port 3000)
- **증상**: 브라우저 접속 시 `@plane/constants` 패키지를 찾을 수 없다는 에러 발생.
- **원인**: 현재 Node.js 버전(**v20.9.0**)이 내부 공유 패키지 빌드 도구(`tsdown`)가 요구하는 최소 버전(**v20.12.0**)보다 낮아 빌드에 실패했습니다.
- **해결 방법**: Node.js 버전을 **v22**로 업데이트하시거나, 도커 데몬을 띄워 컨테이너 환경에서 구동해 주세요.

### 2. 백엔드 구동 실패
- **증상**: 파이썬 장고(Django) 환경 미구축으로 인한 실행 불가.
- **원인**: 로컬에 필요한 라이브러리가 설치되어 있지 않으며, DB 서버(PostgreSQL, Redis)가 도커 데몬 종료로 인해 꺼져 있습니다.
- **해결 방법**: 도커 데몬 실행 후 `docker compose up`을 권장합니다.

롤백은 완벽하게 완료되었으므로, 환경만 맞춰주시면 바로 이어서 작업하실 수 있습니다.
